import styled from "styled-components";

const Navbar = () => {
	return <Background>navbar placeholder</Background>;
};

export default Navbar;

const Background = styled.div`
	background-color: white;
	width: 200px;
`;
